[project]
name = "helpers"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "loguru>=0.7.0",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["helpers"]
package-dir = {"helpers" = "."}